frappe.listview_settings['Document Library'] = {
	// No default filters - the list shows all documents.
	// Clicking DMS Key Metrics number cards applies the appropriate filter via route_options.
	// Users can manually add filters as needed.
};
